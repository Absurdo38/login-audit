<tools>
    <tool id="sqlserver-login-audit" title="SQL Server::login audit" projectTypes="inventory">
        <description>A DBMaster plugin that parses native sql server logs to get aggregated logon statistics</description>

        <parameters>
            <parameter name="p_servers"   title="Servers"   type="string" required="false" multiple="true"
                       suggestion="project_connections" width="300" />
        </parameters>

        <output  script="sqlserver-login-audit.groovy" type="html" />
    </tool>
</tools>
